const StyleCard = () => {
  return (
    <div
      style={{
        backgroundColor: "lightblue",
        padding: "20px",
        borderRadius: "10px",
        color: "darkblue",
      }}
    >
      <h1>Styling in React</h1>
      <p>Working with the inline styles</p>
    </div>
  );
};

export default StyleCard;
