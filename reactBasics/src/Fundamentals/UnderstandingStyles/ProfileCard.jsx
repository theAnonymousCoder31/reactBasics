const ProfileCard = () => {
  const styles = {
    backgroundColor: "lightgrey",
    padding: "15px",
    borderRadius: "8px",
    color: "black",
  };
  return (
    <div style={styles}>
      <h1>Profile Card</h1>
      <p>Working with the styles object</p>
    </div>
  );
};

export default ProfileCard;
